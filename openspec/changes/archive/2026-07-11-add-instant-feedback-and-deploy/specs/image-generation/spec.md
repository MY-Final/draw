## ADDED Requirements

### Requirement: 请求即时上屏
系统 SHALL 在用户发起生成的瞬间即把该次请求呈现在对话流中(用户请求与"生成中"回复占位),并立即清空输入框,而非等待生成完成后才显示。生成完成后,该占位 SHALL 就地更新为成功/失败/空的最终状态。

#### Scenario: 发起即上屏
- **WHEN** 用户点击生成
- **THEN** 系统立即在对话流追加该次请求(含用户 prompt 与"生成中"回复占位),并清空输入框,不等待接口返回

#### Scenario: 完成后就地更新
- **WHEN** 该次生成的接口返回结果
- **THEN** 系统就地将该条占位更新为最终状态(成功展示结果图 / 失败展示错误 / 空展示提示),不新增重复条目

#### Scenario: 失败不留幽灵条目
- **WHEN** 该次生成失败
- **THEN** 系统将同一条占位更新为失败态并显示错误,不遗留无状态的空占位

### Requirement: 生成耗时显示
系统 SHALL 记录每次生成的耗时,并在回复卡上展示:生成过程中实时计秒,完成后定格显示最终耗时。

#### Scenario: 生成中实时计秒
- **WHEN** 一条生成正在进行
- **THEN** 回复卡显示自发起以来的实时耗时(随时间跳动)

#### Scenario: 完成后定格
- **WHEN** 一条生成完成(成功/失败/空)
- **THEN** 系统记录该次耗时并在回复卡定格显示最终秒数
